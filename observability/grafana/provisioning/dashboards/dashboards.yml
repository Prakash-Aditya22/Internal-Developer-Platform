apiVersion: 1

providers:
  - name: 'IDP Dashboards'
    orgId: 1
    folder: 'IDP'
    folderUid: 'idp'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
